apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: immichframe-config
  namespace: immich
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi
